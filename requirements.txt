# =============================================================================
# Nexaura BIS Knowledge Base Builder — Runtime Dependencies
# Install: pip install -r requirements.txt
#
# This file covers BOTH:
#   - bis_ingestion/  (existing metadata crawler)
#   - nexaura/        (PDF intelligence pipeline)
# =============================================================================

# --- Existing bis_ingestion dependencies (DO NOT REMOVE) --------------------
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.0
pydantic>=2.7
pymongo>=4.8
motor>=3.5
fastapi>=0.115
uvicorn>=0.30
rapidfuzz>=3.9

# --- PDF Processing ---------------------------------------------------------
pymupdf>=1.24.0            # PyMuPDF — PDF analysis, text extraction, page rendering

# --- Gemini AI (OCR + Document Intelligence) --------------------------------
google-generativeai>=0.8.0  # Gemini Flash API — primary OCR + document parser

# --- PostgreSQL --------------------------------------------------------------
psycopg2-binary>=2.9.9     # PostgreSQL driver (sync)
asyncpg>=0.29.0            # PostgreSQL async driver

# --- Qdrant Vector Database --------------------------------------------------
qdrant-client>=1.10.0      # Qdrant Python client

# --- Embeddings --------------------------------------------------------------
sentence-transformers>=3.1.0  # Local offline embedding model
numpy>=1.26.0

# --- Search ------------------------------------------------------------------
rank-bm25>=0.2.2           # BM25 keyword search

# --- Configuration & Utilities -----------------------------------------------
PyYAML>=6.0.1              # YAML config loading
python-dotenv>=1.0.0       # .env file loading
xxhash>=3.4.1              # Fast file/page hashing for dedup & caching
tqdm>=4.66.0               # Progress bars
rich>=13.7.0               # Rich terminal output

# --- Dev / Test --------------------------------------------------------------
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.23.0
